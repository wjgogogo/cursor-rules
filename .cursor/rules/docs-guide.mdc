---
description: 
globs: 
alwaysApply: true
---
# 文档书写规范

## Markdown 格式

【必须】遵循：
- 标题层级：`#` → `##` → `###` → `####`，不跳级，不超过四级
- 列表：无序用 `-`，有序用 `1.`，嵌套用四空格缩进
- 代码：三反引号包裹代码块，指定语言，内联用单反引号
- 表格：保持列对齐，使用标准 Markdown 语法

## 中英文排版

【必须】遵循：
- 中英文间加半角空格
- 中文使用全角标点
- 英文数字使用半角字符

## 语言风格

【必须】使用：
- 中文编写，直接简洁的语气
- 短句优先，避免长句
- 删除累赘词："的"、"是"、"了"、"因为"等
- 专业术语保持一致

### 示例对比

```md
<!-- ❌ 冗余表达 -->
前段时间因为采访的缘故，我认识了一个名叫粥粥的餐厅店长。

<!-- ✅ 精简表达 -->
前段时间采访，我认识了个餐厅店长，叫粥粥。
```

## 错别字检查

【必须】避免常见错别字：

| 错误 ❌ | 正确 ✅ | 说明 |
|--------|--------|------|
| 帐号 | 账号 | 涉及账目用"账" |
| 做为 | 作为 | 表示身份、地位用"作为" |
| 像像 | 向向 / 像像 | 根据语境区分"方向"和"相似" |
| 在...中 | 在...中 | 避免"在...当中"的冗余 |
| 的确定 | 确定 | 删除多余的"的" |
| 已经完成了 | 已完成 | 避免"了"的冗余 |
| 进行配置 | 配置 | 直接用动词，删除"进行" |
| 数据库连接 | 数据库连接 | "连接"不要写成"联接" |
| 默认值 | 默认值 | "默认"不要写成"缺省" |
| 点击按钮 | 点击按钮 | "按钮"不要写成"按扭" |

### 技术术语一致性

【必须】统一术语表达：

```md
<!-- ❌ 不一致表达 -->
用户名、使用者名称、账户名

<!-- ✅ 统一表达 -->
用户名（全文统一使用一个术语）
```

常见技术术语规范：
- 数据库 ✅（不用"资料库"）
- 配置 ✅（不用"设定"）
- 函数 ✅（不用"函式"）
- 对象 ✅（不用"物件"）
- 参数 ✅（不用"叁数"）

## 代码示例

【必须】确保：
- 语法正确，可直接运行
- 包含必要的导入语句
- TypeScript 类型完整

```tsx
// ✅ 完整示例
import { useState } from 'react';

interface User {
  id: string;
  name: string;
}

function UserList() {
  const [users, setUsers] = useState<User[]>([]);
  return <div>{users.length} 个用户</div>;
}
```

## 文档更新

【必须】同步：
- 代码变更时更新相关文档
- 重大变更使用警告框标注版本

```md
> **注意**：自 v2.0.0 起，此 API 已更改行为。
```

## 🔍 检查清单

在编写或更新任何 Markdown 文档时，【必须】确认以下各项均符合本规范要求：

- [ ] **[P0]** 内容准确性：文档内容是否准确反映了当前代码库的功能和状态？
- [ ] **[P0]** Markdown 格式：是否严格遵循了本文档定义的 Markdown 格式规范（标题、列表、代码块、表格）？
- [ ] **[P0]** 中英文排版：中英文混排是否符合规范（空格、标点、半角/全角字符）？
- [ ] **[P0]** 语言风格：行文是否简洁、直接，避免了冗余词汇，且专业术语使用是否一致？
- [ ] **[P0]** 错别字检查：是否存在常见错别字（如"帐号"→"账号"、"做为"→"作为"、"联接"→"连接"等）？
- [ ] **[P0]** 术语一致性：技术术语在全文中是否保持一致（避免"用户名"、"使用者名称"混用）？
- [ ] **[P0]** 代码示例质量：所有代码示例是否语法正确、可直接运行、包含必要导入且 TypeScript 类型完整？
- [ ] **[P0]** 文档同步：如果文档对应代码发生变更，相关文档是否已同步更新？重大变更是否已标注？
- [ ] **[P0]** 链接有效性：文档中所有的内部链接和外部链接是否均有效且指向正确的目标？
